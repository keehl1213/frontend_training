import React from "react";
import ReactDOM from "react-dom";


function header() {
    
    return (
  
      <header className="App-header">
        <font className="App-title">TODO&nbsp;<strong>LIST</strong></font>
        <p className="App-title" style={{ fontSize: "20px" }}>A Sample todolist built react hook & context</p>
      </header>
    );
  }

  export default header;
